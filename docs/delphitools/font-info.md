# font-info

Extract metadata from a font file.

## Inputs

- `FONT` required: `ttf`, `otf`, `woff`, or `woff2` font file.

## Options

- Global: `--json`, `--quiet`, `--output`.

## Output

Font metadata.
